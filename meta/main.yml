galaxy_info:
  author: PySlackers
  description: NGINX role with support for Let's Encrypt
  company: PySlackers

  license: MIT

  min_ansible_version: 2.1
  
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions: 9

  galaxy_tags:
    - pyslackers
    - nginx

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
